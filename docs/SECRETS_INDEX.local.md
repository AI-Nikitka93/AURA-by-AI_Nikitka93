# Local Secrets Index

## Known Secret/Access Locations
- Cloudflare auth: local Wrangler OAuth session
- GitHub push auth: relies on locally configured Git credential helper or cached credentials

## Notes
- No raw secret values recorded here.
- If deploy requires switching Cloudflare accounts or tokens, update this file with the storage location only.
