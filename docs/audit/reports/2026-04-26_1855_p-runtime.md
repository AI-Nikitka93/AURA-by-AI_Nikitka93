# RUNTIME AUDIT: AURA by AI_Nikitka93

**Дата:** 2026-04-26 18:55 (+03:00)
**Режим:** STANDARD
**Runtime Score:** 74/100

## 🚦 ОБЩИЙ ВЕРДИКТ
- Статус: PASS WITH WARNINGS
- Core flow broken: no
- Границы verdict: лендинг реально собирается, открывается и проходит ключевой demo-flow `consent -> advisor brief -> waitlist -> privacy/export/clear`. Но финальный внешний outcome для waitlist не подтвержден: локальный и публичный worker сейчас работают в `device queue`, а повторная отправка той же заявки создаёт дубли в очереди.

## Тестовые данные и sandbox
- Локальный интеграционный контур: `http://127.0.0.1:8790` через `npx wrangler dev --local`.
- First-run из README подтверждён через `npm run build`; `vite preview` поднялся на `http://127.0.0.1:4177/`, потому что `4173-4176` уже были заняты в среде.
- Тестовые адреса: `qa-runtime@example.com`, `qa-timeout@example.com`.
- Публичный `POST` в production waitlist не выполнялся намеренно, чтобы не загрязнять реальную очередь тестовыми лидами.

## 🗺️ JOURNEY TABLE

| Flow | Статус | Доказательство | Проблема |
|---|---|---|---|
| First Run: build + local open | PASS | `npm run build` -> `vite build` OK; `GET /` на `127.0.0.1:8790` -> `200`; screenshot `output/playwright/runtime-home-initial.png` | — |
| Consent / first success moment | PASS | consent banner скрывается после `Принять всё`; screenshot `output/playwright/runtime-home-initial.png` | — |
| Advisor brief | PASS | screenshot `output/playwright/runtime-flow1-advisor-brief.png`; network `POST /api/aura-signal -> 502`, но UI выдаёт локальный brief | удалённый AI в local worker не исполняется, flow держится на fallback |
| Waitlist valid submit | PASS WITH WARNINGS | screenshot `output/playwright/runtime-flow2-queued-success.png`; `POST /api/waitlist -> 503`; success state уводит в локальную очередь | внешний relay не подключён, реальная доставка заявки не доказана |
| Waitlist invalid / missing privacy | PASS | screenshot `output/playwright/runtime-flow2-invalid-email.png`; screenshot `output/playwright/runtime-flow2-missing-privacy.png` | — |
| Timeout fallback | PASS | screenshot `output/playwright/runtime-flow-timeout-queue.png`; `requestfailed POST /api/waitlist net::ERR_ABORTED`; `pending after timeout = 1` | — |
| Privacy export / clear / reload | PASS | screenshot `output/playwright/runtime-flow3-privacy-center.png`; JSON `output/runtime-audit/downloads/aura-local-data.json`; screenshot `output/playwright/runtime-flow3-cleared-state.png` | — |
| Duplicate queue submit | BROKEN | screenshot `output/playwright/runtime-flow2-duplicate-queue.png`; exported JSON содержит `pending.length = 2` для одной и той же заявки | нет защиты от дублей |
| Standalone privacy page | PASS | screenshot `output/playwright/runtime-privacy-page.png`; worker log `GET /privacy -> 200` | — |

## ❌ BROKEN FLOWS
1. `RUN-001` — duplicate submission в `device queue`: повторная отправка той же заявки молча добавляет второй pending-entry. Это ломает идемпотентность и искажает локальный state до ручной очистки.

## ⚠️ ХРУПКИЕ МЕСТА
1. `RUN-002` — waitlist relay не подключён: локальный worker и публичный worker `GET /api/waitlist` отвечают `acceptingSubmissions: false`, `mode: device`. Пользователь видит success-screen, но внешний результат не достигается.
2. `RUN-003` — local worker health-check по AI даёт ложноположительный сигнал: `GET /api/aura-signal` возвращает `available: true`, а реальный `POST /api/aura-signal` падает с `502` и `Binding AI needs to be run remotely`. UI спасается fallback-логикой, но capability-сигнал вводит в заблуждение.

## Runtime Summary
- **Runtime status:** лендинг работоспособен как privacy-first demo с локальной очередью и fallback AI.
- **Главный подтверждённый working flow:** `consent -> advisor brief -> submit -> queued success -> export -> clear -> reload`.
- **Главный runtime-блокер:** отсутствие защиты от дублей в локальной очереди.
- **Главный следующий шаг:** ввести идемпотентность для queue-entry и отдельно решить, должен ли waitlist оставаться локальным fallback или реально уходить во внешний relay.

## 🛠️ FIX FIRST
1. Добавить защиту от дублей в `queueWaitlistSubmission`/`submitWaitlist` уровне: хотя бы по fingerprint из `email + ritual + intensity + wearMoment + ecosystem + fitPreference` с явным сообщением пользователю.
2. Привести `/api/aura-signal` capability-check к реальной исполнимости: в local dev не возвращать `workers-ai active`, если binding не может выполнить `run()`.
3. Либо подключить реальный upstream relay для waitlist, либо ещё честнее переименовать success-copy так, чтобы пользователь не воспринимал локальное сохранение как отправку наружу.

## Safe GET recheck на дату аудита
- Public demo из README: `https://ai-nikitka93.github.io/AURA-by-AI_Nikitka93/` -> `200`.
- Public worker waitlist: `https://aura-portfolio-worker.aiartbora.workers.dev/api/waitlist` -> `{"acceptingSubmissions":false,"mode":"device",...}`.
- Public worker AI capability: `https://aura-portfolio-worker.aiartbora.workers.dev/api/aura-signal` -> `{"available":true,"mode":"workers-ai",...}`.

## Если во время аудита были исправления
- Исправления во время аудита не выполнялись. Проверка проведена без изменения runtime-логики продукта.

## 🧠 AUDIT MEMORY HANDOFF
- Report Path: `docs/audit/reports/2026-04-26_1855_p-runtime.md`
- Opened / Updated IDs: `RUN-001`, `RUN-002`, `RUN-003`
- Status Changes: `RUN-001=open`, `RUN-002=accepted_risk`, `RUN-003=open`
- Recheck Owner: `P-RUNTIME — АУДИТ РАНТАЙМА И СЦЕНАРИЕВ.md`

## Артефакты
- Browser audit JSON: `output/runtime-audit/browser-audit.json`
- Trace: `output/runtime-audit/runtime-trace.zip`
- Key screenshots:
  - `output/playwright/runtime-home-initial.png`
  - `output/playwright/runtime-flow1-advisor-brief.png`
  - `output/playwright/runtime-flow2-queued-success.png`
  - `output/playwright/runtime-flow2-duplicate-queue.png`
  - `output/playwright/runtime-flow3-privacy-center.png`
  - `output/playwright/runtime-flow-timeout-queue.png`
